#include<stdio.h>
#include<conio.h>
void main()
{
char ch;
printf("enter the letter",ch);
scanf("%c",&ch);
}
if((ch>65 && ch<=90)||(ch>=92 && ch<=122))
printf("%c is a alphet",ch);
else
printf("%c is a not alphet",ch);
return 0;
}
